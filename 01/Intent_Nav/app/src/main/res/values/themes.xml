<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Intent_Nav" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="materialButtonStyle">@style/Button.Nav.Style</item>
    </style>

    <style name="Theme.Intent_Nav" parent="Base.Theme.Intent_Nav" />
</resources>